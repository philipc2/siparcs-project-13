#!/bin/bash
#PBS -N daxpy_job
#PBS -A NVST0001
#PBS -q main
#PBS -l walltime=00:30:00
#PBS -l select=1:ncpus=16
#PBS -l job_priority=regular

### Set temp to scratch
export TMPDIR=${SCRATCH}/${USER}/temp && mkdir -p $TMPDIR

### Specify desired module environment 
module load conda 
conda activate intro-to-hpc

# List Comp
echo "list_comp 100000000" >> timed_16core.txt
python daxpy.py -n 100000000 -m list_comp >> timed_16core.txt

# Numpy
echo >> timed_16core.txt
echo "numpy 100000000" >> timed_16core.txt
python daxpy.py -n 100000000 -m numpy >> timed_16core.txt

# Numba
echo >> timed_16core.txt
echo "numba 100000000" >> timed_16core.txt
python daxpy.py -n 100000000 -m numba >> timed_16core.txt

#Dask 
echo >> timed_16core.txt
echo "dask 100000000" >> timed_16core.txt
python daxpy.py -n 100000000 -m dask >> timed_16core.txt
